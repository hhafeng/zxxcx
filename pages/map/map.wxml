<!--pages/map/map.wxml-->
<view class='map-page'>
  <map id="map" longitude="{{lng}}" latitude="{{lat}}" scale="14" markers="{{markers}}" show-location style="width: 100%; height: {{windowHeight}}px;"></map>
</view>
